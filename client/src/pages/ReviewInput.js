import React from 'react';
import ReviewForm from '../components/ReviewForm';

const ReviewInput = () => {
    return (
        <main>
                <ReviewForm />
        </main>
    )
};

export default ReviewInput;